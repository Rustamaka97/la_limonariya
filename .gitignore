.DS_Store
.playwright-mcp/

node_modules/
dist/
.env
docker-compose.override.yml

# Raw Clopos API harvest — contains secrets (terminal tokens, admin_password,
# device password 11111) + customer/staff PII. Local seed source only, do NOT commit.
docs/clopos-api/
.gstack/

# Шифрланган бэкаплар — репога кирмайди
backups/
*.sql.gz.enc

# Claude Code: local state stays out, shared skills go in
.claude/*
!.claude/skills/

# Claude MCP config — dev DB connection (parol ochiq)
.mcp.json

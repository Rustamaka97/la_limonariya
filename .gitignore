.DS_Store
.playwright-mcp/

node_modules/
dist/
.env
docker-compose.override.yml

# Raw Clopos API harvest — contains secrets (terminal tokens, admin_password,
# device password 11111) + customer/staff PII. Local seed source only, do NOT commit.
docs/clopos-api/
.gstack/

# DB dumps — contain staff/customer PII + PIN hashes. Local only, do NOT commit.
backups/

# Electron build chiqishi
apps/desktop/dist/
apps/desktop/build/
apps/desktop/icon.png
apps/desktop/config.json
